    </div>
  </main>
  
  <footer class="main-footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-logo">
          <span class="logo-text">MixTrip</span>
          <span class="logo-tagline">วางแผนทริปท่องเที่ยวของคุณ</span>
        </div>
        
        <nav class="footer-nav">
          <ul class="footer-nav-list">
            <li class="footer-nav-item"><a href="<%= baseUrl %>" class="footer-nav-link">หน้าหลัก</a></li>
            <% if (locals.isAuthenticated) { %>
              <li class="footer-nav-item"><a href="<%= baseUrl %>trips" class="footer-nav-link">ทริปของฉัน</a></li>
              <li class="footer-nav-item"><a href="<%= baseUrl %>places" class="footer-nav-link">สถานที่ท่องเที่ยว</a></li>
            <% } else { %>
              <li class="footer-nav-item"><a href="<%= baseUrl %>login" class="footer-nav-link">เข้าสู่ระบบ</a></li>
              <li class="footer-nav-item"><a href="<%= baseUrl %>register" class="footer-nav-link">สมัครสมาชิก</a></li>
            <% } %>
          </ul>
        </nav>
        
        <div class="footer-credits">
          <p>&copy; <%= new Date().getFullYear() %> MixTrip. All rights reserved.</p>
        </div>
      </div>
    </div>
  </footer>

  <script src="<%= baseUrl %>js/main.js"></script>
  <% if (typeof additionalScripts !== 'undefined' && additionalScripts) { %>
    <% additionalScripts.forEach(function(script) { %>
      <script src="<%= baseUrl %><%= script %>"></script>
    <% }); %>
  <% } %>
</body>
</html>